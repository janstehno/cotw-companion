// Copyright (c) 2022 Jan Stehno

enum ObjectType { reserve, animal, caller, weapon, ammo }

enum MapObjectType { outpost, lookout, hide, zone }

enum WeaponType { rifle, shotgun, handgun, bow }

enum DateType { json, format, compare }
